# Local disposable fixture credentials only.
# Set these as shell environment variables for BOTH Docker Compose and Maven.
# Copying to .env alone does not configure Java.
DB_HOST=localhost
DB_PORT=55432
DB_NAME=qa_fixture
DB_USER=qa_local
DB_PASSWORD=qa_local_only

